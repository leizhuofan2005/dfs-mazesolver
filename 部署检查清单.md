# 部署检查清单

## ✅ 已完成

- [x] GitHub Secrets 已配置
- [x] GitHub Actions 工作流已推送
- [x] 构建已触发

## ⏳ 进行中

- [ ] GitHub Actions 构建中（约 5-10 分钟）

## 📋 检查清单

### 1. 检查构建状态

访问：https://github.com/leizhuofan2005/dfs-mazesolver/actions

- [ ] 查看最新的工作流运行
- [ ] 确认构建状态为 "running" 或 "completed"
- [ ] 如果失败，查看日志并修复

### 2. 验证镜像已构建

访问：https://cr.console.aliyun.com

- [ ] 进入命名空间：`algorithm-games`
- [ ] 进入仓库：`algorithm-games`
- [ ] 点击 "镜像版本" 标签
- [ ] 确认 `latest` 标签的镜像存在
- [ ] 镜像地址：`crpi-yvms4cndr4xq7550.cn-hangzhou.personal.cr.aliyuncs.com/algorithm-games/algorithm-games:latest`

### 3. 准备 ACK 部署

- [ ] 获取 ACK 集群的 kubeconfig
  - 访问：https://cs.console.aliyun.com
  - 选择集群 → 连接信息
  - 复制 kubeconfig，保存为 `kubeconfig.yaml`

### 4. 部署到 ACK

运行部署脚本：

```powershell
$env:KUBECONFIG = "kubeconfig.yaml"
.\deploy-and-get-url.ps1
```

### 5. 获取访问地址

部署完成后，脚本会显示访问地址，或运行：

```powershell
kubectl get svc algorithm-games-service -n default
```

## 🚀 快速命令

### 检查构建状态
```powershell
.\check-build-status.ps1
```

### 部署并获取地址
```powershell
.\deploy-and-get-url.ps1
```

### 查看服务状态
```powershell
kubectl get svc algorithm-games-service -n default
kubectl get pods -n default -l app=algorithm-games
```

## 📝 时间线

1. **现在**：GitHub Actions 正在构建（5-10 分钟）
2. **构建完成后**：镜像自动推送到 ACR
3. **验证镜像**：在 ACR 控制台确认镜像存在
4. **部署到 ACK**：运行部署脚本
5. **获取地址**：脚本自动显示访问地址

## 🆘 如果构建失败

1. 检查 GitHub Secrets 是否正确
2. 查看 GitHub Actions 日志
3. 确认 ACR 密码是否正确
4. 检查 Dockerfile 是否有错误

---

**当前状态**：等待 GitHub Actions 构建完成


