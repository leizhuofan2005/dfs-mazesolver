# 修复 403 Forbidden 错误

## 🔍 问题

错误：`403 Forbidden` - 认证失败

**原因**：标准 ACR 可能不支持使用 AccessKey 登录，需要使用账号密码。

## ✅ 解决方案

### 方案 1: 使用账号密码（推荐）

标准 ACR 通常需要使用阿里云账号邮箱和 ACR 服务密码。

#### 步骤 1: 获取 ACR 密码

1. **登录 ACR 控制台**
   - https://cr.console.aliyun.com

2. **获取访问凭证**
   - 点击 **访问凭证** 或 **Access Credentials**
   - 查看或重置密码
   - 复制密码

#### 步骤 2: 更新 GitHub Secrets

访问：https://github.com/leizhuofan2005/dfs-mazesolver/settings/secrets/actions

更新 Secrets：

**ALIYUN_USERNAME**:
- 值：你的**完整阿里云账号邮箱**
- 例如：`hi312*****@aliyun.com`

**ALIYUN_ACR_PASSWORD**:
- 值：**ACR 服务密码**（不是 AccessKey）
- 从 ACR 控制台的"访问凭证"中获取

#### 步骤 3: 重新触发构建

在 GitHub Actions 页面点击 "Re-run jobs"

---

### 方案 2: 使用 ACR 临时令牌

如果账号密码也不工作，可以使用阿里云 CLI 获取临时令牌。

#### 使用阿里云 CLI 获取令牌

```bash
# 安装阿里云 CLI
npm install -g @alicloud/cli

# 配置
aliyun configure set --profile default --mode AK --region cn-hangzhou --access-key-id LTAI5tFDDZiMKb29RrdPSU3h --access-key-secret Q0TbTjlio3msQKMDqcTPTQNTOE2oac

# 获取临时令牌
aliyun cr GetAuthorizationToken --Region cn-hangzhou
```

然后将临时令牌作为密码使用。

---

### 方案 3: 检查标准 ACR 命名空间

403 错误也可能是因为标准 ACR 中没有命名空间。

1. **登录 ACR 控制台**
   - https://cr.console.aliyun.com

2. **切换到标准实例**（不是个人版）

3. **创建命名空间**
   - 命名空间：`algorithm-games`
   - 如果不存在，创建它

4. **创建镜像仓库**
   - 仓库名称：`algorithm-games`
   - 如果不存在，创建它

---

### 方案 4: 使用个人版 ACR（如果标准版不可用）

如果标准 ACR 不可用，可以继续使用个人版，但需要在本地构建：

```powershell
# 使用个人版域名（本地可以解析）
docker login --username=你的账号邮箱 crpi-yvms4cndr4xq7550.cn-hangzhou.personal.cr.aliyuncs.com
docker build -t algorithm-games .
docker tag algorithm-games crpi-yvms4cndr4xq7550.cn-hangzhou.personal.cr.aliyuncs.com/algorithm-games/algorithm-games:latest
docker push crpi-yvms4cndr4xq7550.cn-hangzhou.personal.cr.aliyuncs.com/algorithm-games/algorithm-games:latest
```

---

## 📋 快速检查清单

- [ ] 已获取 ACR 服务密码（不是 AccessKey）
- [ ] GitHub Secrets 已更新为账号邮箱和 ACR 密码
- [ ] 标准 ACR 中有命名空间 `algorithm-games`
- [ ] 标准 ACR 中有镜像仓库 `algorithm-games`

---

**推荐：使用方案 1（账号密码），这是最可靠的方法！**


