# 立即执行构建 - 操作指南

## ⚠️ 当前状态

Docker Desktop 未运行，需要先启动。

## 🚀 执行步骤

### 步骤 1: 启动 Docker Desktop

1. **打开开始菜单**
2. **搜索 "Docker Desktop"**
3. **点击启动**
4. **等待 Docker 完全启动**（任务栏图标不再闪烁，通常需要 30-60 秒）

### 步骤 2: 验证 Docker 运行

打开 PowerShell，运行：

```powershell
docker ps
```

如果显示容器列表（即使是空的），说明 Docker 已运行。

### 步骤 3: 执行构建

在项目目录运行：

```powershell
cd C:\Users\think\Documents\GitHub\dfs-mazesolver
.\build-and-push.ps1
```

脚本会：
1. 检查 Docker ✅
2. 提示输入账号邮箱和 ACR 密码
3. 登录 ACR
4. 构建镜像（5-10 分钟）
5. 推送镜像

### 步骤 4: 输入凭证

脚本会提示输入：
- **阿里云账号邮箱**：例如 `hi312*****@aliyun.com`
- **ACR 服务密码**：从 https://cr.console.aliyun.com → 访问凭证 获取

### 步骤 5: 等待构建完成

构建需要 5-10 分钟，请耐心等待。

### 步骤 6: 部署到 ACK

构建完成后，运行：

```powershell
.\deploy-and-get-url.ps1
```

---

## 📋 快速命令（复制粘贴）

```powershell
# 1. 启动 Docker Desktop（在开始菜单）

# 2. 验证 Docker
docker ps

# 3. 执行构建
cd C:\Users\think\Documents\GitHub\dfs-mazesolver
.\build-and-push.ps1

# 4. 部署（构建完成后）
.\deploy-and-get-url.ps1
```

---

## ⚡ 如果 Docker 已运行

如果 Docker Desktop 已经在运行，直接执行：

```powershell
cd C:\Users\think\Documents\GitHub\dfs-mazesolver
.\build-and-push.ps1
```

---

**提示**：确保 Docker Desktop 已完全启动后再执行构建脚本！


